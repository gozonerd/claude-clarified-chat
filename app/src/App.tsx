import React from 'react';

export function App(): React.ReactElement {
  return (
    <div className="app-landing">
      <h1>Claude Clarified Chat</h1>
      <p>
        Claude Clarified Chat un-black-boxes your Claude conversation exports.
        Drop in a Claude zip export and instantly browse your full thread
        history in a clean, accessible timeline — then export to PDF, DOCX,
        XLSX, or Markdown. Everything runs locally in your browser; no data
        leaves your machine.
      </p>
    </div>
  );
}
